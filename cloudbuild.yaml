steps:
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: bash
    args:
      - '-c'
      - |
        echo ${SHORT_SHA} && \
        echo ${COMMIT_SHA} && \
    id: "Read FILES"
 options:
 logging: CLOUD_LOGGING_ONLY
